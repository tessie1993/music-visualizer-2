# Provenance of the prebuilt libraries committed in this directory.
#
# They are produced by .github/workflows/native-libs.yml (manual dispatch):
# projectM at the tag named in the run, patched with
# tools/projectm-v417-render-fbo-backport.patch, plus the JNI bridge
# tools/pm_jni.c. When committing rebuilt .so files, commit the SHA256SUMS
# from the same workflow artifact - it carries the run URL and source SHA,
# which is what lets this repo prove the blobs match the sources.
# android.yml verifies these hashes on every pull request.
#
# Build run: unknown (these blobs predate provenance tracking; tools/pm_jni.c
# has been hardened since - rebuild via native-libs.yml at the next release).
5dee4cacbefe5d324cbdf05f6281daa629586a6e0c3ed1a52d2eef4e328f136f  libprojectM-4.so
2aa9031982edc116bf1d4be93f1d44bbc6716225953d617e1c76b0643235b252  libprojectmjni.so
